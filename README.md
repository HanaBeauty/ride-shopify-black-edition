# ride-shopify-black-edition

## Development

### Testing

Install dependencies once with `npm install`, then run the JavaScript test suite with:

```bash
npm test
```

The suite is powered by Vitest and focuses on the JavaScript helpers located in `assets/`.
